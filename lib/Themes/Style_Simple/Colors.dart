import 'package:flutter/material.dart';

// ===========================================================
// BRAND COLORS
// ===========================================================
const Color primaryColor = Color(0xFF0F4C81); // Navy Blue
const Color accentColor = Color(0xFF00ACC1); // Cyan
const Color premiumColor = Color(0xFF7C4DFF); // Purple

// ===========================================================
// FUNCTIONAL COLORS
// ===========================================================
const Color successColor = Color(0xFF26A69A); // Teal
const Color warningColor = Color(0xFFFFB300); // Gold
const Color errorColor = Color(0xFFEF5350); // Coral (Destructive)

// ===========================================================
// NEUTRAL PALETTE
// ===========================================================
const Color backgroundColor = Color(0xFFFAFBFC);
const Color white1Color = Color(0xFFFFFFFF);
const Color surfaceColor = Color(0xFFF6F8FA);
const Color borderColor = Color(0xFFE8EAED);
const Color textColor = Color(0xFF24292F);
const Color text1Color = Color(0xFF57606A);
const Color text2Color = Color(0xFF8B949E);

// ===========================================================
// APP BAR
// ===========================================================
const Color barTitleColor = primaryColor;


class AppColors {
  // Brand colors
  static const Color primary = Color(0xFF0F4C81);
  static const Color accent = Color(0xFF00ACC1);
  static const Color premium = Color(0xFF7C4DFF);

  // Functional colors
  static const Color success = Color(0xFF26A69A);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFEF5350);

  // Neutrals
  static const Color background = Color(0xFFFAFBFC);
  static const Color white = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF6F8FA);
  static const Color border = Color(0xFFE8EAED);
  static const Color textPrimary = Color(0xFF24292F);
  static const Color textSecondary = Color(0xFF57606A);
  static const Color textTertiary = Color(0xFF8B949E);

  // App bar
  static const Color barTitle = primary;
}
